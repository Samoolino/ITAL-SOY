[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false
ffi = false

[fmt]
line_length = 100

[rpc_endpoints]
controlled_fork = "${CONTROLLED_FORK_RPC_URL}"
